<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.wanji.business.mapper.TjDeviceMapper">

    <resultMap id="BaseResultMap" type="net.wanji.business.entity.TjDevice">
            <id property="deviceId" column="device_id" jdbcType="INTEGER"/>
            <result property="deviceName" column="device_name" jdbcType="VARCHAR"/>
            <result property="ip" column="ip" jdbcType="VARCHAR"/>
            <result property="serviceAddress" column="service_address" jdbcType="VARCHAR"/>
            <result property="dataChannel" column="data_channel" jdbcType="VARCHAR"/>
            <result property="commandChannel" column="command_channel" jdbcType="VARCHAR"/>
            <result property="createdBy" column="created_by" jdbcType="VARCHAR"/>
            <result property="createdDate" column="created_date" jdbcType="TIMESTAMP"/>
            <result property="updatedBy" column="updated_by" jdbcType="VARCHAR"/>
            <result property="updatedDate" column="updated_date" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        device_id,device_name,ip,
        service_address,data_channel,command_channel,
        created_by,created_date,updated_by,
        updated_date
    </sql>

    <select id="selectByCondition" resultType="net.wanji.business.entity.TjDevice">
        SELECT d.*
        FROM tj_device d
        <where>
            <if test="deviceName != null and deviceName !='' ">
                AND d.device_name LIKE concat('%', #{deviceName}, '%')
            </if>
            <if test="deviceType != null and deviceType != '' ">
                AND d.device_type = #{deviceType}
            </if>
            <if test="status != null">
                AND d.status = #{status}
            </if>
        </where>
    </select>
</mapper>
